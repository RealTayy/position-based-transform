- Incorporate jQuery targeting for initialization
- Do I need to calc the hypo? Probably not
- use getComputedStyle() & getPropertyValue() to determine starting transform values so you can use library on properties with transforms already on them.